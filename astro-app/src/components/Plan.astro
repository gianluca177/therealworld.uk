---
/* SECTION: OUR PLAN -- Premium dark + orange accents */
const steps = [
  {
    n: 1,
    title: "Foundation",
    copy:
      "Launch a fast, compliant site with clear structure, simple messaging, and live CTAs -- built to stay stable under ads.",
    bullets: [
      "Pages: Home, About, Interviews, FAQ, Terms, Privacy, Contact.",
      "Live CTAs linked to affiliate URL with clean UTM tags.",
      "Policy-safe copy (no earnings claims, transparent disclaimers).",
    ],
  },
  {
    n: 2,
    title: "Education First",
    copy:
      "Teach practical modern skills with short lessons, clear checklists, and community support -- designed for execution.",
    bullets: [
      "Structured paths for beginners -- start executing day one.",
      "Searchable library (1,000+ lessons & resources as it grows).",
      "Mentor guidance + global community support.",
    ],
  },
  {
    n: 3,
    title: "Execution & Feedback",
    copy:
      "Apply what you learn on real tasks, gather feedback, iterate, and build momentum -- with accountability built in.",
    bullets: [
      "Small real tasks → feedback → iteration → momentum.",
      "Student interviews for social proof (no guaranteed outcomes).",
      "Updates as platforms and best practices evolve.",
    ],
  },
];
---

<section id="plans" class="bg-[#000000] text-white scroll-mt-28">
  <div class="max-w-6xl mx-auto px-4 py-20">
    <h2 class="text-3xl md:text-4xl font-bold text-center text-[var(--color-orange)] tracking-tight">
      What is our plan?
    </h2>

    <p class="mt-4 text-center text-[var(--color-text-muted)] max-w-3xl mx-auto leading-relaxed">
      Keep it simple: teach useful skills, help members execute, and improve through feedback.
      No promises -- just a clear path from learning to applying.
    </p>

    <div class="mt-12 grid gap-8 md:grid-cols-3">
      {steps.map((s) => (
        <div class="h-full rounded-2xl border border-white/10 bg-[#121212] p-6 md:p-7 transition hover:border-[#FF8C2A33] hover:bg-[#141414]">
          <div class="flex h-full flex-col">
            {/* Header */}
            <div class="flex items-center justify-between">
              <div class="flex items-center gap-3">
                <span class="inline-flex items-center justify-center h-9 w-9 rounded-full bg-[#FF8C2A14] border border-[#FF8C2A33] text-[var(--color-orange)] text-sm font-semibold">
                  {s.n}
                </span>
                <div class="leading-tight">
                  <p class="text-xs uppercase tracking-wide text-white/50">Step {s.n}</p>
                  <h3 class="text-lg font-semibold text-[var(--color-text-light)]">
                    {s.title}
                  </h3>
                </div>
              </div>

              {/* small accent */}
              <span class="h-2 w-2 rounded-full bg-[#FF8C2A] opacity-70"></span>
            </div>

            {/* Copy */}
            <p class="mt-5 text-sm leading-relaxed text-[var(--color-text-muted)]">
              {s.copy}
            </p>

            {/* Separator */}
            <div class="mt-6 h-px bg-white/10"></div>

            {/* Bullets (push to bottom for consistent card feel) */}
            <ul class="mt-6 space-y-3 text-sm text-[var(--color-text-muted)]">
              {s.bullets.map((b) => (
                <li class="flex items-start gap-3">
                  <span class="mt-2 h-1.5 w-1.5 rounded-full bg-[#FF8C2A]"></span>
                  <span>{b}</span>
                </li>
              ))}
            </ul>

            {/* Spacer to ensure equal feel on all cards */}
            <div class="mt-auto"></div>
          </div>
        </div>
      ))}
    </div>

    <div class="mt-14 flex flex-col sm:flex-row justify-center gap-4">
      <a href="https://jointherealworld.com/checkout?a=7nrnndpb" class="btn">
        Get Started
      </a>

      <a
        href="#interviews"
        class="inline-flex items-center justify-center px-6 py-3 rounded-full border border-white/15 text-white/80 font-semibold hover:border-[#FF8C2A33] hover:text-white transition"
      >
        See Interviews
      </a>
    </div>

    <p class="mt-5 text-center text-xs text-white/40">
      Educational platform -- we don’t guarantee earnings. Your results depend on your effort and circumstances.
    </p>
  </div>
</section>
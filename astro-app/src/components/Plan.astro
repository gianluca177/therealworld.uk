---

const steps = [
  {
    n: 1,
    title: "Choose a Skill",
    copy:
      "Once inside The Real World, you choose a single skill to focus on. Members get full access to all courses, but progress is fastest when you commit to one clear path.",
    bullets: [
      "Access 10+ modern skill tracks including AI, e-commerce, marketing, and copywriting.",
      "1,000+ professionally produced lessons updated as strategies evolve.",
      "Direct access to community chats and mentors inside each skill campus.",
    ],
  },
  {
    n: 2,
    title: "Learn & Apply",
    copy:
      "You don’t just watch videos. You apply what you learn immediately using proven frameworks, examples from other members, and real-time feedback.",
    bullets: [
      "Step-by-step learning paths designed for execution, not theory.",
      "See how other members build, test, and improve real projects.",
      "Ask questions, share progress, and learn best practices from active operators.",
    ],
  },
  {
    n: 3,
    title: "Execution & Accountability",
    copy:
      "Progress compounds through consistent execution, feedback, and accountability. You track wins, refine strategy, and stay focused through community pressure and momentum.",
    bullets: [
      "Execution-first culture with visible progress and shared wins.",
      "Accountability through leaderboards, challenges, and peer discussion.",
      "Guidance from experienced mentors on improving systems and decision-making.",
    ],
  },
];
---

<section id="plans" class="bg-[#050b16] py-28">
  <div class="max-w-6xl mx-auto px-6">

    <!-- TITLE -->
    <div class="text-center">

      <h2 class="text-3xl md:text-4xl font-semibold tracking-wide">
        <span class="bg-gradient-to-r from-[#e9b20a] to-[#ffaa30] bg-clip-text text-transparent">
          Hustlers University’s Plan
        </span>
      </h2>

      <!-- SUBTITLE -->
      <p class="mt-6 text-gray-400 max-w-2xl mx-auto leading-relaxed">
        The Real World follows a simple
        <span class="bg-gradient-to-r from-[#e9b20a] to-[#ffaa30] bg-clip-text text-transparent opacity-90">
          execution loop
        </span>
        —
        <span class="bg-gradient-to-r from-[#e9b20a] to-[#ffaa30] bg-clip-text text-transparent opacity-90">
          choose
        </span>
        a skill,
        <span class="bg-gradient-to-r from-[#e9b20a] to-[#ffaa30] bg-clip-text text-transparent opacity-90">
          apply
        </span>
        it in the real world,
        then
        <span class="bg-gradient-to-r from-[#e9b20a] to-[#ffaa30] bg-clip-text text-transparent opacity-90">
          improve
        </span>
        through feedback and accountability.
        <span class="bg-gradient-to-r from-[#e9b20a] to-[#ffaa30] bg-clip-text text-transparent opacity-90">
          No shortcuts.
        </span>
        Just a clear system for progress.
      </p>

    </div>

    <!-- CARDS -->
    <div class="mt-20 grid gap-10 md:grid-cols-3">
      {steps.map((s) => (
        <div class="relative group">

          <!-- Refined Glow -->
          <div
            class="absolute inset-0 rounded-2xl pointer-events-none
                   opacity-50 transition duration-300
                   group-hover:opacity-100"
            style="
              box-shadow:
                0 0 10px rgba(255,160,35,1),
                0 0 20px rgba(255,160,35,.45);
            "
          ></div>

          <!-- Card -->
          <div
            class="relative rounded-2xl p-8 h-full overflow-hidden
                   bg-[#0B0F1A]
                   border border-[rgba(255,160,35,.6)]
                   transition duration-300
                   group-hover:border-[#ffa023]
                   group-hover:bg-[#111827]"
          >

            <!-- Subtle Inner Highlight -->
            <div
              class="absolute inset-0 rounded-2xl pointer-events-none
                     opacity-50 transition duration-300
                     group-hover:opacity-100"
              style="box-shadow: inset 0 0 12px rgba(255,170,48,.25);"
            ></div>

            <!-- Subtle Grid -->
            <div
              class="absolute inset-0 opacity-[0.035] pointer-events-none"
              style="
                background-image:
                linear-gradient(to right, white 1px, transparent 1px),
                linear-gradient(to bottom, white 1px, transparent 1px);
                background-size: 52px 52px;
              "
            ></div>

            <div class="relative z-10 flex h-full flex-col">

              <div class="flex items-start gap-4">

                <span
                  class="flex items-center justify-center w-9 h-9 text-sm font-semibold text-black rounded-full shrink-0"
                  style="background: linear-gradient(99.43deg, #ffbc39 10.06%, #ff8d3a 104.9%);"
                >
                  {s.n}
                </span>

                <div class="leading-tight">
                  <p class="text-[11px] tracking-widest text-[#ffaa30] uppercase mb-1">
                    STEP {s.n}
                  </p>

                  <h3 class="text-base font-semibold text-white">
                    {s.title}
                  </h3>
                </div>

              </div>

              <p class="mt-5 text-sm leading-relaxed text-gray-400">
                {s.copy}
              </p>

              <div class="mt-6 h-px bg-white/10"></div>

              <ul class="mt-6 space-y-4 text-sm text-gray-300">
                {s.bullets.map((b) => (
                  <li class="flex items-start gap-3">
                    <svg
                      class="mt-1 w-4 h-4 text-emerald-400 shrink-0"
                      fill="none"
                      stroke="currentColor"
                      stroke-width="2"
                      viewBox="0 0 24 24"
                    >
                      <path stroke-linecap="round" stroke-linejoin="round"
                        d="M5 13l4 4L19 7" />
                    </svg>
                    <span>{b}</span>
                  </li>
                ))}
              </ul>

              <div class="mt-auto"></div>
            </div>
          </div>
        </div>
      ))}
    </div>

    <!-- CTA -->
    <div class="mt-24 flex justify-center">
      <a
        href="https://jointherealworld.com/checkout?a=7nrnndpb"
        class="btn px-12 py-4 text-base tracking-wide"
      >
        JOIN HUSTLERS UNIVERSITY
      </a>
    </div>

  </div>
</section>

<!-- MOBILE CLICK TO ACTIVATE TITLE -->
<script>
  document.addEventListener("DOMContentLoaded", () => {
    const wrapper = document.querySelector(".plan-title-wrapper");

    if (!wrapper) return;

    wrapper.addEventListener("click", (e) => {
      e.stopPropagation();
      wrapper.classList.add("is-active");
    });

    document.addEventListener("click", () => {
      wrapper.classList.remove("is-active");
    });
  });
</script>

---
/* SECTION: OUR PLAN -- Black + Gold/Orange Palette */
const steps = [
  {
    n: "Step 1",
    title: "Foundation",
    copy:
      "Ship a fast, compliant site on Cloudflare (Astro + Tailwind). Clear copy, simple structure, and live CTAs. No hype -- just what members get and how to start.",
  },
  {
    n: "Step 2",
    title: "Education First",
    copy:
      "Guide members through practical, modern skills: marketing, copywriting, e-commerce, AI-assisted workflows. Short lessons, clear checklists, and community Q&A.",
  },
  {
    n: "Step 3",
    title: "Execution & Feedback",
    copy:
      "Apply skills on small real tasks, gather feedback, iterate. Keep momentum with accountability, interviews, and updates as platforms change.",
  },
];
---
<section id="plans" class="bg-[#000000] text-white">
  <div class="max-w-6xl mx-auto px-4 py-16">
    <h2 class="text-3xl font-bold text-center text-[#FF8C2A]">WHAT IS OUR PLAN?</h2>

    <p class="mt-3 text-center text-[#B3B3B3] max-w-3xl mx-auto">
      Keep it simple: launch the platform, teach useful skills, and help members execute. No promises --
      just a clear path from learning to applying.
    </p>

    <div class="mt-10 grid md:grid-cols-3 gap-6">
      {steps.map((s) => (
        <div class="group p-6 rounded-lg border border-[#FF8C2A33] bg-[#1A1A1A] transition hover:border-[#FF8C2A66] hover:bg-[#1A1A1A]/80">
          <div class="flex items-center gap-3">
            <span class="inline-flex h-8 w-8 items-center justify-center rounded-full bg-[#FF8C2A22] border border-[#FF8C2A55] text-[#FF8C2A] text-xs font-semibold">
              {s.n}
            </span>
            <h3 class="font-bold text-white">{s.title}</h3>
          </div>

          <p class="mt-3 text-[#B3B3B3] text-sm leading-relaxed">
            {s.copy}
          </p>

          <!-- tiny separator -->
          <div class="mt-5 h-px bg-gradient-to-r from-transparent via-[#FF8C2A33] to-transparent"></div>

          <!-- bullets -->
          <ul class="mt-4 space-y-2 text-[#B3B3B3] text-sm">
            {s.title === "Foundation" && (
              <>
                <li class="flex items-start gap-2">
                  <span class="mt-1 h-2 w-2 rounded-full bg-[#FF8C2A]"></span>
                  <span>Pages: Home, About, Interviews, FAQ, Terms, Privacy, Contact.</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="mt-1 h-2 w-2 rounded-full bg-[#FF8C2A]"></span>
                  <span>Live CTAs linked to affiliate URL with clean UTM tags.</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="mt-1 h-2 w-2 rounded-full bg-[#FF8C2A]"></span>
                  <span>Policy-safe copy (no earnings claims, transparent disclaimers).</span>
                </li>
              </>
            )}
            {s.title === "Education First" && (
              <>
                <li class="flex items-start gap-2">
                  <span class="mt-1 h-2 w-2 rounded-full bg-[#FF8C2A]"></span>
                  <span>Structured paths for beginners -- start executing day one.</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="mt-1 h-2 w-2 rounded-full bg-[#FF8C2A]"></span>
                  <span>Searchable library (1,000+ lessons & resources as it grows).</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="mt-1 h-2 w-2 rounded-full bg-[#FF8C2A]"></span>
                  <span>Mentor guidance + global community support.</span>
                </li>
              </>
            )}
            {s.title === "Execution & Feedback" && (
              <>
                <li class="flex items-start gap-2">
                  <span class="mt-1 h-2 w-2 rounded-full bg-[#FF8C2A]"></span>
                  <span>Small real tasks → feedback → iteration → momentum.</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="mt-1 h-2 w-2 rounded-full bg-[#FF8C2A]"></span>
                  <span>Student interviews for social proof (no guaranteed outcomes).</span>
                </li>
                <li class="flex items-start gap-2">
                  <span class="mt-1 h-2 w-2 rounded-full bg-[#FF8C2A]"></span>
                  <span>Updates as platforms and best practices evolve.</span>
                </li>
              </>
            )}
          </ul>
        </div>
      ))}
    </div>

    <div class="mt-12 flex justify-center gap-4">
      <a
        href="https://jointherealworld.com/?a=7nrnndpb&utm_source=therealworlduk&utm_medium=cta_plans"
        class="px-6 py-3 rounded bg-[#FF8C2A] text-black font-semibold hover:bg-[#FFAE5A] transition shadow-[0_0_12px_rgba(255,140,42,0.35)]"
      >
        Get Started
      </a>
      <a
        href="#interviews"
        class="px-6 py-3 rounded border border-[#FF8C2A] text-[#FF8C2A] font-semibold hover:bg-[#FF8C2A11] transition"
      >
        See Interviews
      </a>
    </div>

    <p class="mt-4 text-center text-xs text-[#777777]">
      Educational platform -- we don’t guarantee earnings. Your results depend on your effort and circumstances.
    </p>
  </div>
</section>